## Default configuration, generated by rosdoc2.

## This 'attic section' self-documents this file's type and version.
type: 'rosdoc2 config'
version: 1

---

settings:
  ## Path (relative to package.xml) to the Python source directory.
  ## rosdoc2 adds its parent to sys.path so that 'import camera_info_manager' works.
  python_source: camera_info_manager

builders:
    - sphinx: {
        name: 'camera_info_manager_py',
        sphinx_sourcedir: doc,
        output_dir: ''
    }
